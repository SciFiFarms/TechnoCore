#!/bin/bash

technocore_folder="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )/../../../../"
if [ -f "${technocore_folder}/../.env" ]; then
    source ${technocore_folder}.env 
fi

source ${TECHNOCORE_LIB:-/usr/local/lib/technocore/}compose.sh
